W63 — the institution conjunct, measured. Two arms, one build, one authored constant apart.
Generated by tools/w63/paired.mjs and tools/w63/identical.mjs over tools/w63/arms.mjs output.
before-arm: ascension-institutions = 0 (the identity value, = origin/main's predicate)
after-arm:  ascension-institutions = 2 (shipped)
40 worlds per strategy: 4 starting positions x 10 replicates, 2400 ticks, rootSeed 20260811.

ARCHIVIST IS n=11 PER ARM, NOT 40, and is reported at that n rather than omitted or rounded up.
Its runs found 1281-1345 universities each and are an order of magnitude slower than the rest of
the pool. BOTH ARCHIVIST ARMS TERMINATED EARLY, at 11 of 40, and the cause is not established:
the processes exited leaving no error in their logs, no stack trace and no non-zero status that
was captured, having been launched detached from a shell that had already exited. An unattended
process that vanishes silently is most consistent with the OS reclaiming it -- these runs hold
over thirteen hundred university records each and were the largest in the pool -- but that is an
inference and not a diagnosis, so it is written here as one. The arm was NOT stopped deliberately
and was not truncated for time.

A re-run was attempted and DELIBERATELY ABANDONED rather than left to finish. Measured pace on the
original arm is ~19 minutes per run, so 40 runs is ~12.7 hours per arm and ~25 hours for the pair.
That is not a cost this branch should pay for a corner it already reports honestly at n=11, and a
result nobody is waiting for is worse than a result nobody has: the branch would sit unreviewed for
a day to firm up a number whose direction is not in question. Anyone who wants the full 40 can run:

    node tools/w63/arms.mjs --arm before --strategies archivist --replicates 10 --ticks 2400 --out <dir>
    node tools/w63/arms.mjs --arm after  --strategies archivist --replicates 10 --ticks 2400 --out <dir>

What the 11 support, and it is a claim about the content ceiling rather than about a sample:
archivist holds exactly 51 nodes in every run measured, in both arms, which is the ceiling of the
starting rectangle it never permits past. All 11 pairs are bit-identical across the two arms.
A universe that permits nothing cannot reach a canon it has no access to, so the remaining 29
replicates are not expected to differ -- but that expectation is untested, and any claim resting
on archivist should be read at n=11. Every other strategy below is the full 40.

=== paired: same worlds, one constant apart ===

strategy               n   qual(before)  qual(after)   before-only  after-only   ascended b/a
denial-warden          40        0/40         0/40             0           0         0/0
idle-then-declare      40        0/40         0/40             0           0         0/0
narrow-depth           40        0/40         0/40             0           0         0/0
open-then-build        40       40/40        40/40             0           0        40/40
passive-control        40        0/40         0/40             0           0         0/0
permissive-breadth     40       40/40         0/40            40           0        40/0
permit-then-idle       40       40/40         0/40            40           0        40/0
portal-rush            40        0/40         0/40             0           0         0/0
uniform-random-legal   40        0/40         0/40             0           0         0/0
worship-maximizer      40        0/40         0/40             0           0         0/0

no anomalies: every strategy paired on identical run seeds, probes inert, no after-only qualification

=== which universes the conjunct changed, by snapshot hash ===

A matching hash means the two arms produced the same world at every tick, not merely
the same metrics. Differing hashes are listed with what changed.

strategy               paired  identical  differing   asc before/after
denial-warden             40         40          0          0/0
idle-then-declare         40         40          0          0/0
narrow-depth              40         40          0          0/0
open-then-build           40         40          0         40/40
passive-control           40         40          0          0/0
permissive-breadth        40          0         40         40/0
permit-then-idle          40          0         40         40/0
portal-rush               40         40          0          0/0
uniform-random-legal      40         40          0          0/0
worship-maximizer         40         40          0          0/0

TOTAL: 320 of 400 paired runs are bit-identical across the two arms.
The complement is the set of universes the conjunct actually touched, and any balance delta
must be attributable to it alone. A delta appearing in a run whose hash matched would be a bug.

=== archivist, from run logs rather than from the analysers above ===

tools/w63/arms.mjs writes its JSON only when a strategy's 40 runs finish, so an arm still in
flight is absent from both tables above. These rows are counted from the per-run stderr lines
instead, which carry the same fields. Stated this way rather than folded in, because a number
from a different source than the table it sits beside should say so.

arm     n    qualified   ascended   universities   nodes known
before  11   0/11        0/11       1281..1345     {51}
after   11   0/11        0/11       1281..1345     {51}

The 2x2 this completes: permit-then-idle opens the grid to 261-269 nodes on ONE university and
fails; archivist builds ~1300 universities on 51 nodes and fails; passive-control does neither
and fails; open-then-build reaches 198-220 nodes on 78-98 universities and qualifies 40/40.
Both single-axis maximisers fail, and the conjunction passes with two orders of magnitude fewer
universities than the strategy that fails. That is a predicate rather than a counter.
