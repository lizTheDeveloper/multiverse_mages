{
  "name": "@mm/lobby",
  "version": "0.9.0",
  "private": true,
  "description": "MMO lobby — matchmaking, universe lifecycle, static file serving. The player's entry point.",
  "license": "AGPL-3.0-or-later",
  "author": "Ann Kelner",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mm/agent-api": "*",
    "@mm/scenario": "*",
    "@mm/server": "*"
  }
}
