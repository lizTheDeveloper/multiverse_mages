{
  "name": "@mm/rules-raid",
  "version": "0.2.0",
  "private": true,
  "description": "Engagement space, combat, objectives, and raid consequences. Skeleton — the rules land in raid-engagement.",
  "license": "AGPL-3.0-or-later",
  "author": "Ann Kelner",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.0.0 <23"
  },
  "//": "Rules-path code: no runtime dependencies, no Node built-ins, integer-only. Per contracts.md §5 this is the coordinating layer that may import BOTH rules-magic and rules-world — and MUST NOT import agent-api.",
  "scripts": {
    "build": "tsc --build"
  }
}
