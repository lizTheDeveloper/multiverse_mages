{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Typechecks the server's tests. Never emits into dist/. The tests build a minimal universe with @mm/sim-core and @mm/state so a match runs against a real agent-api session rather than only a fake one — contracts.md §5 grants both as test-only edges, and src may not touch either. See the testOnly note in sim-core/test/unit/module-boundaries.test.ts.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./test",
    "types": ["node"],
    "emitDeclarationOnly": true,
    "outDir": "../../.tsbuild/server-test",
    "tsBuildInfoFile": "../../.tsbuild/server-test.tsbuildinfo"
  },
  "include": ["test/**/*.ts"],
  "references": [
    { "path": "./tsconfig.json" },
    { "path": "../sim-core" },
    { "path": "../state" }
  ]
}
