{
  "$schema": "https://json.schemastore.org/tsconfig",
  "//": "Library build. `types: []` is deliberate — the observation export runs inside the client and the server alike, so it must not see Node's ambient types.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    "types": []
  },
  "include": ["src/**/*.ts"]
}
