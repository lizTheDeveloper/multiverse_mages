{
  "name": "@mm/agent-api",
  "version": "0.2.0",
  "private": true,
  "description": "Observation and action space, legality masks, slot-indexed candidate lists, and boundary normalization — contracts.md §4.",
  "license": "AGPL-3.0-or-later",
  "author": "Ann Kelner",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.0.0 <23"
  },
  "//": "Not covered by the rules-path float ban: contracts.md §4.1 makes this the ONE place floating point is permitted, on the observation export path. The dependency runs one way — rules packages never import this. @mm/content is deliberately NOT a dependency: §5 would grant it, but content's public surface re-exports a filesystem loader and this package has to run in a browser, so content facts arrive as a caller-supplied catalogue (src/catalogue.ts).",
  "dependencies": {
    "@mm/sim-core": "*",
    "@mm/state": "*"
  },
  "scripts": {
    "build": "tsc --build"
  }
}
