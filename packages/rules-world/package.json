{
  "name": "@mm/rules-world",
  "version": "0.0.0",
  "private": true,
  "description": "Mages, species, populace cohorts, universities, and the economy. Skeleton — the rules land in mages-and-species.",
  "license": "AGPL-3.0-or-later",
  "author": "Ann Kelner",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.0.0 <23"
  },
  "//": "Rules-path code: no runtime dependencies, no Node built-ins, integer-only. Per contracts.md §5 it may import sim-core, content, and state — and MUST NOT import rules-magic or agent-api.",
  "scripts": {
    "build": "tsc --build"
  }
}
